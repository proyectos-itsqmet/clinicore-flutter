<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET lives HERE, not only in src/debug and src/profile.
         The Flutter template puts it in those two variants because the tool
         needs it for hot reload, and that is enough while you are developing.
         It is NOT enough for a release APK: without this line the signed build
         installs, opens, and then cannot reach the API at all. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required by local_auth for BiometricPrompt. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Necesario para que el paciente pueda consultarle al asistente por
         audio (paquete `record`). Sin esta linea el permiso NO se puede pedir
         en tiempo de ejecucion: la app no falla al compilar, falla al grabar,
         y el error se lee como "el microfono no anda". -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:label="${appName}"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
